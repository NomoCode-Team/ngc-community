/**
 * @brief converts keys of an object to values of an array
 * @param obj object to convert
 * @return array of keys from object
 */

export default function convertObjectKeysToArray(obj: object) {
  return Object.keys(obj)
}