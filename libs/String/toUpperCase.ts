/**
 * @brief Converts input string to upper case
 * @param text input string to be converted
 * @return upper case string
 */
 export default function toUpperCase(text: string): string {
  return text.toUpperCase()
}