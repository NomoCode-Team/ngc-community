/**
 * @brief Converts input string to lower case
 * @param text input string to be converted
 * @return  lower case string
 */
export default function toLowerCase(text: string): string {
    return text.toLowerCase()
}