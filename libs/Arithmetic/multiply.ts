
/**
 * @brief Multiplies two numbers
 * @param left left hand side
 * @param right right hand side
 * @speak multiply {left} by {right}
 */
 export default function multiply(left: number, right: number) {
  return left * right
}