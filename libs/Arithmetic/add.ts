
/**
 * @brief Adds two numbers
 * @param left left hand side
 * @param right right hand side
 * @speak add {left} to {right}
 */
 export default function add(left: number, right: number) {
  return left + right
}
