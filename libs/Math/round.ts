/**
 * @brief Returns a supplied numeric expression rounded to the nearest integer.
 * @param x — The value to be rounded to the nearest integer.
 */
 export default function round(x: number) {
    return Math.round(x)
}